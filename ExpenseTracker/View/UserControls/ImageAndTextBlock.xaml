<UserControl x:Class="ExpenseTracker.View.UserControls.ImageAndTextBlock"
             x:Name="root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExpenseTracker.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="45" d:DesignWidth="200">
    <Grid>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <Image Source="{Binding ImagePath, ElementName=root}" Margin="0,0,10,0" Height="auto" Width="auto" Stretch="Uniform"/>
            <TextBlock FontSize="{Binding FontSize, ElementName=root}" VerticalAlignment="Center"
                       Text="{Binding Text, ElementName=root}" Foreground="{Binding Foreground, ElementName=root}"/>
        </StackPanel>
        
    </Grid>
</UserControl>
