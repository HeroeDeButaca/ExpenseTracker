<UserControl x:Class="ExpenseTracker.View.UserControls.ImageButton"
             x:Name="root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ExpenseTracker.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="45" d:DesignWidth="200">
    <Grid>
        <Grid Height="45" Width="45" HorizontalAlignment="Left">
            <Image Source="{Binding ImagePath, ElementName=root}" HorizontalAlignment="Center" Panel.ZIndex="-1"
               Height="auto" Width="auto" Stretch="Uniform"/>
        </Grid>
        <Button Background="Transparent" BorderThickness="0" Command="{Binding Command, ElementName=root}"
                CommandParameter="{Binding CommandParameter, ElementName=root}"/>
        <TextBlock Text="{Binding Text, ElementName=root}" FontSize="{Binding FontSize, ElementName=root}" Foreground="White"
                   VerticalAlignment="Center" Margin="53,0,0,0" Panel.ZIndex="-1"/>
    </Grid>
</UserControl>
