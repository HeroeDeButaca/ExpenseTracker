<Window x:Class="ExpenseTracker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ExpenseTracker"
        xmlns:uc="clr-namespace:ExpenseTracker.View.UserControls"
        mc:Ignorable="d"
        Title="Expense Tracker" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="9*"/>
        </Grid.RowDefinitions>

        <uc:Header/>

        <Grid Grid.Row="1" Background="Gray">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="8*"/>
            </Grid.ColumnDefinitions>

            <uc:Sidebar
                HomeCommand="{Binding SetHomeViewCommand}"
                TransactionsCommand="{Binding SetTransactionsViewCommand}"
                ExchangeCommand="{Binding SetExchangeViewCommand}"
                SettingsCommand="{Binding SetSettingsViewCommand}"/>

            <ContentControl Grid.Column="1"
                            Content="{Binding CurrentView}"/>
        </Grid>
    </Grid>
</Window>
